# Use legacy peer dependency resolution so StackBlitz's bare `npm install`
# behaves the same as the CI workflow's `npm ci --legacy-peer-deps`.
# Without this, npm 7+ strict peer dep resolution can fail on the R3F
# package tree when opening the project in StackBlitz WebContainers.
legacy-peer-deps=true
